# Chapter 8 Handover

## 目的

本文档记录第 8 章 `8_Calibration` 当前已经完成的重写工作、仍然存在的缺口，以及下一步最值得推进的方向。

如果后续需要切换到新会话，建议优先阅读：

1. `Roadmap.md`
2. `Chapter7_Handover.md`
3. `Chapter8_Handover.md`


## 当前定位

第 8 章的目标不是只给出“校准名词解释”，而是把校准写成一个面向训练的章节：

- 把误差模型、求解器直觉和实践流程连接起来；
- 让 1GC、2GC、3GC 的差别不仅体现在标题上，也体现在可运行示例里；
- 为第 9 章的完整数据处理工作流提供理论与算法过渡。


## 本轮已完成内容

本轮已经系统性重写并验证以下 notebook：

- `8_Calibration/8_0_introduction.ipynb`
- `8_Calibration/8_1_calibration_least_squares_problem.ipynb`
- `8_Calibration/8_2_1gc.ipynb`
- `8_Calibration/8_3_2gc.ipynb`
- `8_Calibration/8_4_3gc.ipynb`
- `8_Calibration/8_5_further_reading_and_references.ipynb`
- `8_Calibration/8_problem_set.ipynb`
- `8_Calibration/8_x_further_reading_and_references.ipynb`

配套新增生成脚本：

- `tools/rebuild_chapter8_notebooks.py`

当前这些 notebook 已做顺序执行验证，状态为可运行。


## 已完成的结构升级

与旧版相比，第 8 章目前已经具备以下改进：

- `8.0` 不再只是章节导言，而是明确解释“校准在做什么、为什么求得一个解并不等于得到可信结果”；
- `8.1` 已重写为训练型最小二乘 notebook：
  - 简化 EW 阵列与 $uv$ 覆盖生成
  - 非线性最小二乘目标函数与全局相位退化示例
  - 简化 LM 求解器与收敛历史
  - 参考天线变更后的规范不变性
  - 模型不完整时残差停滞的演示
- `8.2` 已增加训练型内容：
  - 校准源观测节奏示意
  - 点源校准源上的复增益求解示例
  - 解转移到目标场后的剩余误差示例
  - 闭合相位与闭合振幅演示
  - 1GC 实践处理链说明
- `8.3` 已增加训练型内容：
  - 自校准闭环的最小模拟
  - 不完整 sky model 与更完整 sky model 的对比
  - solution interval 对残差的影响
  - 自校准失败模式总结
- `8.4` 已增加训练型内容：
  - 主波束与指向误差导致的方向依赖响应示例
  - 方向无关模型与方向依赖模型的残差对比
  - 3GC 常见路线与升级判断
- `8.5` 已从旧式占位页改成按主题组织的阅读引导页；
- `8_problem_set` 已改成工作簿式训练 notebook，覆盖阻尼参数、参考天线、模型不完整和交替求解器练习；
- `8.x` 目前保留为兼容导航页，用于承接旧链接。


## 当前仍然存在的缺口

第 8 章主体已经完成收尾，当前仍有一些“可继续加厚”的方向，但不再属于必须补上的结构性缺口。

### 1. `8.1` 仍可继续加厚算法深度

`8_1_calibration_least_squares_problem.ipynb` 已完成重写，但后续还可以继续强化：

- 非线性最小二乘的退化与病态性
- 残差向量、雅可比矩阵与收敛诊断
- LM、交替迭代、StEFCal 之间的联系与差异
- 加权最小二乘与异方差噪声

### 2. `8_problem_set.ipynb` 还可以补更多分层任务

当前问题集已经重写，但仍可继续扩展：

- 增加“改参数看失败模式”的训练题；
- 明确区分“推导题、编程题、诊断题”；
- 给出一份教师用或自学用的参考结论提纲。

### 3. 章节末尾导航还有兼容性历史包袱

目前 `8_5` 和 `8_x` 同时存在，是为了兼容旧链接。后续如果全库导航全部清理完成，可以考虑进一步收拢，但当前不必优先处理。


## 与后续章节的衔接判断

当前判断是：

- 第 8 章已经从“旧讲义式校准章节”提升为“可训练的校准章节”；
- `8.1` 与 `8_problem_set` 现在已经纳入同一套训练风格；
- 从全书推进效率看，下一步最值得做的仍然是第 9 章 `9_Practical`，因为那一部分是全书当前最大的结构性缺口。


## 建议的下一步顺序

建议按下面顺序继续：

1. 若还有余力，可继续加厚 `8_1_calibration_least_squares_problem.ipynb` 的算法诊断部分
2. 视情况扩展 `8_problem_set.ipynb` 的分层题目
3. 正式扩写 `9_Practical`，优先建立完整连续谱处理主线

若希望优先追求“全书可用性”，当前已经可以直接进入第 9 章。


## 第 9 章建议起步方向

如果下一步直接进入实践章，建议首先补下面这些 notebook：

1. 数据检查与质量控制
2. 基础 flagging 与异常识别
3. 1GC 基础流程
4. 连续谱基础成像
5. 自校准实战
6. 图像质量评估与基本测量

也就是说，第 9 章应该从“零散演示页”升级成一条完整的数据处理链。


## 工作约束

后续继续修改第 8 章与第 9 章时，仍应遵守以下要求：

- 中文正文为主，英文仅保留必要术语、缩写、软件任务名和变量名；
- notebook 优先使用自包含、可运行示例；
- 尽量使用 `numpy` 和 `matplotlib` 等基础依赖；
- 程序、脚本、notebook 和文档中禁止写死个人机器绝对路径；
- 优先解释物理含义，再解释命令与参数；
- 避免把内容写成泛泛科普，目标始终是专业训练教程。
