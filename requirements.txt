# Python 3.10+ runtime dependencies.
#
# These are the direct dependencies used by the notebooks and teaching
# modules. Version ranges keep the environment installable across supported
# Python 3 releases while preventing incompatible major-version upgrades.

jupyterlab>=4.2,<5
notebook>=7.2,<8
ipython>=8.18,<10
numpy>=2.0,<3
matplotlib>=3.9,<4
scipy>=1.13,<2
astropy>=6.1,<9
ipywidgets>=8.1,<9
pyyaml>=6,<7
